#include <list.h>

int main() {
	List *lst = create)list();

	lst = insert_first(lst, 1);
	lst = insert_first(lst, 4);
	lst = insert_first(lst, 9);
	lst = insert_first(lst, 2);

	print_list(lst);
	free_list(lst);

	return 0;
}